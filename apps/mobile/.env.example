# Copy to .env and fill in. These are PUBLIC (baked into the build).
# The deployed API origin (no trailing slash) — native must use an absolute URL.
EXPO_PUBLIC_API_BASE_URL=https://your-app.vercel.app
# Clerk publishable key (the same project as the web app).
EXPO_PUBLIC_CLERK_PUBLISHABLE_KEY=pk_test_xxx

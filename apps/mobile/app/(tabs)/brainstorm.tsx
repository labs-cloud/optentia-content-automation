import { ScreenPlaceholder } from "@/components/ScreenPlaceholder";

export default function Brainstorm() {
  return (
    <ScreenPlaceholder
      title="Brainstorm"
      note="Phase 1: the native Tinder swipe deck (Gesture Handler + Reanimated, reusing useSwipeDeck)."
    />
  );
}

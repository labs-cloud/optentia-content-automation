import { ScreenPlaceholder } from "@/components/ScreenPlaceholder";

export default function Calendar() {
  return (
    <ScreenPlaceholder title="Calendar" note="Phase 2: scheduled content calendar / agenda view." />
  );
}

import { ScreenPlaceholder } from "@/components/ScreenPlaceholder";

export default function Campaigns() {
  return (
    <ScreenPlaceholder title="Campaigns" note="Phase 2: campaign list + detail screens." />
  );
}

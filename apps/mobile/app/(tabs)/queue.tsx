import { ScreenPlaceholder } from "@/components/ScreenPlaceholder";

export default function Queue() {
  return (
    <ScreenPlaceholder
      title="Approval queue"
      note="Phase 1: approval cards with platform/status chips and approve/reject actions."
    />
  );
}

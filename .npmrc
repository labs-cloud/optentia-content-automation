# React Native / Expo + pnpm monorepos need a hoisted node_modules so Metro and
# Babel can resolve plugins/presets (pnpm's default symlinked layout breaks them).
# Vercel installs only the root web member (--filter .), so this is safe for the
# web/server deploy too.
node-linker=hoisted
